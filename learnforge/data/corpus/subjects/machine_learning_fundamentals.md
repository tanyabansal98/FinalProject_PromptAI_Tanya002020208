# Machine Learning Fundamentals
## Core concepts
ML is learning patterns from data. Three types: supervised (labeled), unsupervised (unlabeled), reinforcement (rewards). Key pipeline: data→preprocess→features→model→evaluate→deploy→monitor.
## Common misconceptions
- ML ≠ AI (ML is a subset of AI)
- More data doesn't always help (quality > quantity)
- Models don't 'understand' — they find statistical patterns
## Key algorithms
Linear/logistic regression, decision trees, random forests, SVMs, k-means, neural networks, gradient boosting.
